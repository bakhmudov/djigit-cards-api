services:
    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        container_name: cards-phpmyadmin
        restart: always
        ports:
            - '8080:80'
        environment:
            PMA_HOST: ${DB_HOST}
            PMA_PORT: ${DB_PORT}
            MYSQL_ROOT_PASSWORD: ""
